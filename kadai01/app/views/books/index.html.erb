<h1>書籍一覧</h1>

<% @books.each do |book| %>
  <p>
    <%= book.title %>（<%= book.author %>／<%= book.published_year %>）
    <%= link_to '表示', "/books/#{book.id}" %>
    <%= link_to '編集', "/books/#{book.id}/edit" %>
    <%= link_to '削除', "/books/#{book.id}", data: { turbo_method: :delete } %>
  </p>
<% end %>

<p><%= link_to '新規追加', '/books/new' %></p>
